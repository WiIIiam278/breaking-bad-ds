#pragma once

enum Direction
{
    LEFT,
    DOWN,
    RIGHT,
    UP
};

enum Tile
{
    VOID,
    FLOOR,
    WALL
};

enum Mode
{
    TITLE_SCREEN,
    DIALOGUE,
    MOVE,
    MINIGAME
};

enum Speaker
{
    GALE  
};